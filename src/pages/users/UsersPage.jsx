import { CrudPage } from "../../components/ui/CrudPage.jsx";import { useApiList } from "../../hooks/useApiList.js";import { userApi } from "../../services/userApi.js";
export function UsersPage(){const {rows,loading,error}=useApiList(userApi.list);return <CrudPage title="Users" eyebrow="DIRECTORY" description="Create, maintain and review Keycloak-backed local user references." rows={rows} loading={loading} error={error} columns={[{key:"usernameCache",label:"Username",render:r=>r.usernameCache||r.username||r.userName||"-"},{key:"emailCache",label:"Email",render:r=>r.emailCache||r.email||"-"},{key:"organizationId",label:"Organization"},{key:"active",label:"Status",render:r=><span className={`badge ${r.active!==false?"badge-green":"badge-gray"}`}>{r.active!==false?"Active":"Inactive"}</span>}]} />}
