import { CrudPage } from "../../components/ui/CrudPage.jsx";import { useApiList } from "../../hooks/useApiList.js";import { userApi } from "../../services/userApi.js";
export function UserRolesPage(){const {rows,loading,error}=useApiList(userApi.list);return <CrudPage title="User Roles" eyebrow="MAPPING" description="Select a user to review or assign roles." rows={rows} loading={loading} error={error} columns={[{key:"usernameCache",label:"User",render:r=>r.usernameCache||r.username||"-"},{key:"emailCache",label:"Email",render:r=>r.emailCache||r.email||"-"},{key:"id",label:"Local User Ref ID"}]} />}
