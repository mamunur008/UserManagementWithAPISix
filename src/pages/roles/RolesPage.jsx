import { CrudPage } from "../../components/ui/CrudPage.jsx";import { useApiList } from "../../hooks/useApiList.js";import { roleApi } from "../../services/roleApi.js";
export function RolesPage(){const {rows,loading,error}=useApiList(roleApi.list);return <CrudPage title="Roles" eyebrow="ACCESS" description="Manage local role cache mapped to Keycloak realm roles." rows={rows} loading={loading} error={error} columns={[{key:"nameCache",label:"Name",render:r=>r.nameCache||r.name||"-"},{key:"description",label:"Description"},{key:"isGlobal",label:"Global",render:r=><span className="badge badge-blue">{r.isGlobal?"Yes":"No"}</span>},{key:"isElevated",label:"Elevated",render:r=><span className="badge badge-gray">{r.isElevated?"Yes":"No"}</span>}]} />}
