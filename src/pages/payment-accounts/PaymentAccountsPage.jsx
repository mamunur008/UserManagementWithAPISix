import { CrudPage } from "../../components/ui/CrudPage.jsx";import { useApiList } from "../../hooks/useApiList.js";import { paymentAccountApi } from "../../services/paymentAccountApi.js";
export function PaymentAccountsPage(){const {rows,loading,error}=useApiList(paymentAccountApi.list);return <CrudPage title="Payment Accounts" eyebrow="FINANCE" description="Manage bank, mobile wallet and cash accounts by organization." rows={rows} loading={loading} error={error} columns={[{key:"holder",label:"Holder"},{key:"type",label:"Type"},{key:"isDefault",label:"Default",render:r=><span className={`badge ${r.isDefault?"badge-green":"badge-gray"}`}>{r.isDefault?"Default":"No"}</span>},{key:"active",label:"Status",render:r=><span className="badge badge-green">{r.active===false?"Inactive":"Active"}</span>}]} />}
