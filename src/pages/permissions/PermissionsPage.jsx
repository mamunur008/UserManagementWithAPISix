import { CrudPage } from "../../components/ui/CrudPage.jsx";import { useApiList } from "../../hooks/useApiList.js";import { permissionApi } from "../../services/permissionApi.js";
export function PermissionsPage(){const {rows,loading,error}=useApiList(permissionApi.list);return <CrudPage title="Permissions" eyebrow="ACCESS" description="Manage permission definitions used by role-permission mappings." rows={rows} loading={loading} error={error} columns={[{key:"name",label:"Name"},{key:"description",label:"Description"},{key:"active",label:"Status",render:r=><span className="badge badge-green">{r.active===false?"Inactive":"Active"}</span>}]} />}
