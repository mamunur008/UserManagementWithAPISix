import { CrudPage } from "../../components/ui/CrudPage.jsx";import { useApiList } from "../../hooks/useApiList.js";import { organizationApi } from "../../services/organizationApi.js";
export function OrganizationsPage(){const {rows,loading,error}=useApiList(organizationApi.list);return <CrudPage title="Organizations" eyebrow="TENANCY" description="Manage organization hierarchy and partner/vendor organizations." rows={rows} loading={loading} error={error} columns={[{key:"name",label:"Name"},{key:"typeId",label:"Type ID"},{key:"parentId",label:"Parent"},{key:"active",label:"Status",render:r=><span className="badge badge-green">{r.active===false?"Inactive":"Active"}</span>}]} />}
