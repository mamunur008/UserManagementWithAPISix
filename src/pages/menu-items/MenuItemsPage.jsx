import { CrudPage } from "../../components/ui/CrudPage.jsx";import { useApiList } from "../../hooks/useApiList.js";import { menuItemApi } from "../../services/menuItemApi.js";
export function MenuItemsPage(){const {rows,loading,error}=useApiList(menuItemApi.list);return <CrudPage title="Menu Items" eyebrow="NAVIGATION" description="Server-side menu configuration and menu-role assignments." rows={rows} loading={loading} error={error} columns={[{key:"name",label:"Name"},{key:"url",label:"URL"},{key:"icon",label:"Icon"},{key:"active",label:"Status",render:r=><span className="badge badge-green">{r.active===false?"Inactive":"Active"}</span>}]} />}
