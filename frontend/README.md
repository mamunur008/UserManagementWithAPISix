# UserManagement Frontend

React + Vite + Tailwind CSS v4 frontend for UserManagement.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Environment

Copy `.env.example` to `.env` and adjust URLs if needed.

Theme colors are in `src/styles.css` under `@theme`.
