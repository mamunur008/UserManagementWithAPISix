import { CrudPage } from '../../components/ui/CrudPage.jsx';
import { paymentAccountApi } from '../../services/paymentAccountApi.js';
export function PaymentAccountsPage(){ return <CrudPage title="Payment Accounts" eyebrow="PAYMENT" description="Manage organization bank, mobile and cash accounts." api={paymentAccountApi} createPermission="permission:paymentaccounts.create" updatePermission="permission:paymentaccounts.update" deletePermission="permission:paymentaccounts.delete" columns={[{key:'holder',label:'Holder'},{key:'type',label:'Type'},{key:'organizationId',label:'Organization'},{key:'isDefault',label:'Default',render:x=><span className={`badge ${x.isDefault?'badge-green':'badge-gray'}`}>{x.isDefault?'Default':'No'}</span>}]} formFields={[{name:'organizationId',label:'Organization Id'},{name:'type',label:'Type'},{name:'holder',label:'Holder'},{name:'active',label:'Active',type:'checkbox',defaultValue:true}]} /> }
