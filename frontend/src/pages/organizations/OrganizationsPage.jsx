import { CrudPage } from '../../components/ui/CrudPage.jsx';
import { organizationApi } from '../../services/organizationApi.js';
export function OrganizationsPage(){ return <CrudPage title="Organizations" eyebrow="ORGANIZATION" description="Manage organization hierarchy." api={organizationApi} createPermission="permission:organizations.create" updatePermission="permission:organizations.update" deletePermission="permission:organizations.delete" columns={[{key:'name',label:'Name'},{key:'typeId',label:'Type'},{key:'parentId',label:'Parent'},{key:'active',label:'Status',render:x=><span className={`badge ${x.active===false?'badge-gray':'badge-green'}`}>{x.active===false?'Inactive':'Active'}</span>}]} formFields={[{name:'name',label:'Name'},{name:'typeId',label:'Type Id'},{name:'parentId',label:'Parent Id'},{name:'commissionRate',label:'Commission Rate'},{name:'active',label:'Active',type:'checkbox',defaultValue:true}]} /> }
