import { CrudPage } from '../../components/ui/CrudPage.jsx';
import { permissionApi } from '../../services/permissionApi.js';
export function PermissionsPage(){ return <CrudPage title="Permissions" eyebrow="AUTHORIZATION" description="Manage permission keys used by pages, buttons and backend policies." api={permissionApi} createPermission="permission:permissions.create" updatePermission="permission:permissions.update" deletePermission="permission:permissions.delete" columns={[{key:'key',label:'Key',render:x=>x.key||x.name||x.code||'-'},{key:'description',label:'Description'},{key:'active',label:'Status',render:x=><span className={`badge ${x.active===false?'badge-gray':'badge-green'}`}>{x.active===false?'Inactive':'Active'}</span>}]} formFields={[{name:'key',label:'Permission Key',lockOnEdit:true},{name:'name',label:'Name'},{name:'description',label:'Description'},{name:'active',label:'Active',type:'checkbox',defaultValue:true}]} /> }
